您说得非常对，做 Demo
阶段最忌讳陷入"大而全"的泥潭。为了快速打通核心链路，我们需要做极致的"减法"和"模拟"。

针对您"快速落地、功能打通"的目标，我为您重新梳理了一份《MVP 最小可行性
Demo
需求设计》。这份文档砍掉了所有复杂的后台配置、用户权限和真实支付环节，核心只保留"输入条件
-\> 算法生成 -\> 前端可视化"这一条主线，确保能在极短时间内看到效果。

**核心设计原则：前端模拟，逻辑先行**

为了追求速度，Demo
阶段**不开发复杂的后台管理系统**。所有的场馆数据、基础定价规则、行业系数等，直接以
JSON 文件的形式在前端或本地 Mock
服务中硬编码。我们的目标是验证"展位推荐+动态定价"这个核心算法逻辑能否跑通，以及可视化效果是否达标。

**功能模块一：极简参数输入面板**

这是 Demo 的"控制台"，只需提供三个最核心的下拉或输入选项，用于触发算法：

1.  选定场馆：提供 2-3 个预设的 Demo
    场馆（如"国际会展中心A馆"、"科技博览中心B馆"），每个场馆预设好固定的长宽尺寸和基础底图。

2.  展览行业：提供 3-4
    个典型行业标签（如"人工智能"、"新能源"、"传统制造"、"文创消费"），每个标签对应一个预设的"行业热度系数"。

3.  展区方位偏好：提供简单的单选或滑块（如"核心主通道"、"次级通道"、"边角区域"），用于模拟用户的心理预期。

**功能模块二：核心算法引擎（本地 Mock 版）**

这是 Demo
的"大脑"，不需要对接真实的历史交易数据，只需编写一套简化的加权计算公式：

1.  展位示意图生成逻辑：根据选定的场馆，直接调用预设的 SVG 或 Canvas
    底图。根据"展区方位偏好"，在底图上高亮或标记出系统推荐的 10-20
    个虚拟展位坐标。

2.  动态定价计算逻辑：采用公式 最终报价 = 基础底价 × 方位系数 ×
    行业热度系数 × 实时供需模拟因子。其中，"实时供需模拟因子"在 Demo
    中可以设置为一个随机波动值（例如 0.9 到 1.2
    之间），用来模拟真实环境下的动态变化效果，让价格看起来是"活"的。

**功能模块三：可视化交互与结果展示**

这是 Demo 的"门面"，重点在于直观呈现：

1.  交互式展位示意图：使用前端 Canvas 或 SVG
    技术渲染场馆平面图。用户鼠标悬停在推荐展位上时，弹出浮层显示该展位的详细信息（展位号、面积、具体方位）。

2.  动态价格查阅面板：当用户点击某个推荐展位时，右侧面板实时显示该展位的动态定价。为了体现"动态"感，可以设计一个简单的动画效果，让价格在
    1-2
    秒内从"基础价"跳动到"最终推荐价"，并附带一行小字解释（如："因人工智能行业热度高，溢价
    20%"）。

3.  导出/下一步按钮：Demo
    阶段无需做真实的预订流程，点击"确认展位"后，只需弹出一个模拟成功的
    Toast 提示，或将选中的展位 ID 和价格打印在控制台即可。

**落地执行建议**

为了确保快速出活，建议技术团队采用以下策略：前端直接使用 Vue/React 配合
ECharts 或 Konva.js 进行 Canvas 绘图；后端甚至可以直接省去，将所有的场馆
JSON 数据和定价算法封装在前端的 JS 工具类中。这样，整个 Demo
可以作为一个纯前端项目独立运行，彻底规避了数据库建表、接口联调和服务器部署的时间成本。

这份需求设计将原本宏大的系统浓缩为了一个"算法验证器"，既保留了您最核心的展位推荐和动态定价逻辑，又具备了极高的可落地性。您看这个简化后的
MVP
范围是否符合您当前的预期？如果需要，我可以进一步为您细化那个"动态定价公式"的具体参数，或者为您起草一份前端
Canvas 绘图的简易技术方案。
